// Niri Configuration - FOCUS MODE (Minimal)
// No gaps, instant animations, maximum screen real estate
// Toggle with Super+Shift+V

// ============================================================================
// INPUT CONFIGURATION
// ============================================================================

input {
    keyboard {
        xkb {
            layout "us"
        }
        repeat-delay 300
        repeat-rate 50
    }

    touchpad {
        tap
        dwt
        natural-scroll
        accel-speed 0.0
        accel-profile "adaptive"
    }

    mouse {
        accel-speed 0.0
        accel-profile "adaptive"
    }

    focus-follows-mouse
    workspace-auto-back-and-forth
}

// ============================================================================
// OUTPUT CONFIGURATION
// ============================================================================

output "eDP-1" {
    scale 1.0
}

// ============================================================================
// LAYOUT - FOCUS MODE (no gaps, maximum space)
// ============================================================================

layout {
    gaps 0

    center-focused-column "never"

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }

    default-column-width { proportion 0.5; }

    // Subtle focus indicator
    focus-ring {
        width 1
        active-color "#f97316"
        inactive-color "#161b22"
    }

    border {
        width 1
        active-color "#30363d"
        inactive-color "#0d1117"
    }
}

// ============================================================================
// SPAWN AT STARTUP
// ============================================================================

// waybar is started by DMS, don't duplicate
spawn-at-startup "mako"
spawn-at-startup "sh" "-c" "wl-paste --watch cliphist store"

// ============================================================================
// ENVIRONMENT VARIABLES
// ============================================================================

environment {
    QT_QPA_PLATFORM "wayland"
    QT_WAYLAND_DISABLE_WINDOWDECORATION "1"
    ELECTRON_OZONE_PLATFORM_HINT "auto"
    MOZ_ENABLE_WAYLAND "1"
    XCURSOR_THEME "Bibata-Modern-Ice"
    XCURSOR_SIZE "24"
}

// ============================================================================
// PREFER NO CSD
// ============================================================================

prefer-no-csd

// ============================================================================
// SCREENSHOT PATH
// ============================================================================

screenshot-path "~/Pictures/Screenshots/Screenshot from %Y-%m-%d %H-%M-%S.png"

// ============================================================================
// ANIMATIONS - FOCUS MODE (instant, minimal)
// ============================================================================

animations {
    slowdown 0.5

    window-open {
        duration-ms 80
        curve "ease-out-quad"
    }

    window-close {
        duration-ms 60
        curve "ease-out-quad"
    }

    window-resize {
        duration-ms 50
        curve "ease-out-quad"
    }

    window-movement {
        duration-ms 80
        curve "ease-out-quad"
    }

    workspace-switch {
        duration-ms 100
        curve "ease-out-quad"
    }

    horizontal-view-movement {
        duration-ms 100
        curve "ease-out-quad"
    }

    config-notification-open-close {
        duration-ms 80
        curve "ease-out-quad"
    }
}

// ============================================================================
// WINDOW RULES
// ============================================================================

window-rule {
    match app-id="pavucontrol"
    default-column-width { fixed 600; }
}

window-rule {
    match app-id="org.gnome.Calculator"
    default-column-width { fixed 400; }
}

window-rule {
    match app-id="blueman-manager"
    default-column-width { fixed 500; }
}

// ============================================================================
// KEYBINDINGS (same as Vibe mode)
// ============================================================================

binds {
    // Navigation
    Mod+Space { spawn "wofi" "--show" "drun"; }
    Mod+W { close-window; }
    Mod+F { fullscreen-window; }
    Mod+O { toggle-window-floating; }
    Mod+Alt+F { maximize-column; }

    // Workspaces
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }

    Mod+Tab { focus-workspace-down; }
    Mod+Shift+Tab { focus-workspace-up; }

    Mod+Shift+1 { move-window-to-workspace 1; }
    Mod+Shift+2 { move-window-to-workspace 2; }
    Mod+Shift+3 { move-window-to-workspace 3; }
    Mod+Shift+4 { move-window-to-workspace 4; }
    Mod+Shift+5 { move-window-to-workspace 5; }
    Mod+Shift+6 { move-window-to-workspace 6; }
    Mod+Shift+7 { move-window-to-workspace 7; }
    Mod+Shift+8 { move-window-to-workspace 8; }
    Mod+Shift+9 { move-window-to-workspace 9; }

    // Focus
    Mod+Left  { focus-column-left; }
    Mod+Down  { focus-window-down; }
    Mod+Up    { focus-window-up; }
    Mod+Right { focus-column-right; }
    Mod+H { focus-column-left; }
    Mod+J { focus-window-down; }
    Mod+K { focus-window-up; }
    Mod+L { focus-column-right; }

    // Move windows
    Mod+Shift+Left  { move-column-left; }
    Mod+Shift+Down  { move-window-down; }
    Mod+Shift+Up    { move-window-up; }
    Mod+Shift+Right { move-column-right; }
    Mod+Shift+H { move-column-left; }
    Mod+Shift+J { move-window-down; }
    Mod+Shift+K { move-window-up; }
    Mod+Shift+L { move-column-right; }

    // Resize
    Mod+Equal { set-column-width "+10%"; }
    Mod+Minus { set-column-width "-10%"; }
    Mod+Shift+Equal { set-window-height "+10%"; }
    Mod+Shift+Minus { set-window-height "-10%"; }

    // Column grouping
    Mod+T { consume-or-expel-window-left; }
    Mod+G { consume-window-into-column; }
    Mod+Alt+G { expel-window-from-column; }

    // Apps
    Mod+Return { spawn "kitty"; }
    Mod+Shift+B { spawn "brave"; }
    Mod+Shift+F { spawn "thunar"; }
    Mod+Shift+M { spawn "spotify"; }
    Mod+Shift+N { spawn "code"; }
    Mod+Shift+O { spawn "obsidian"; }
    Mod+Shift+Slash { spawn "bitwarden-desktop"; }
    Mod+Shift+D { spawn "kitty" "-e" "lazygit"; }
    Mod+Ctrl+T { spawn "kitty" "-e" "btop"; }

    // TUI Apps
    Mod+E { spawn "kitty" "-e" "yazi"; }
    Mod+Shift+C { spawn "kitty" "-e" "cava"; }
    Mod+Shift+P { spawn "kitty" "-e" "ncspot"; }
    Mod+Ctrl+D { spawn "kitty" "-e" "lazydocker"; }

    // Mode Toggle
    Mod+Shift+V { spawn "sh" "-c" "~/.local/bin/toggle-mode"; }

    // System
    Mod+Ctrl+A { spawn "pavucontrol"; }
    Mod+Ctrl+B { spawn "blueman-manager"; }
    Mod+Ctrl+W { spawn "nm-connection-editor"; }
    Mod+Ctrl+L { spawn "swaylock"; }
    Mod+Escape { quit; }
    Mod+Ctrl+R { spawn "niri" "msg" "action" "load-config-file"; }

    // Screenshots & Clipboard (all save to file + clipboard)
    Mod+Shift+S { spawn "sh" "-c" "grim -g \"$(slurp)\" - | tee ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png | wl-copy"; }
    Print { spawn "sh" "-c" "grim -g \"$(slurp)\" - | tee ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png | wl-copy"; }
    Mod+Print { spawn "sh" "-c" "grim - | tee ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png | wl-copy"; }
    Mod+Ctrl+V { spawn "sh" "-c" "cliphist list | wofi --dmenu | cliphist decode | wl-copy"; }

    // Media
    XF86AudioRaiseVolume { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%+"; }
    XF86AudioLowerVolume { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%-"; }
    XF86AudioMute        { spawn "wpctl" "set-mute" "@DEFAULT_AUDIO_SINK@" "toggle"; }
    XF86AudioMicMute     { spawn "wpctl" "set-mute" "@DEFAULT_AUDIO_SOURCE@" "toggle"; }
    XF86AudioPlay  { spawn "playerctl" "play-pause"; }
    XF86AudioPause { spawn "playerctl" "play-pause"; }
    XF86AudioNext  { spawn "playerctl" "next"; }
    XF86AudioPrev  { spawn "playerctl" "previous"; }

    // Mouse
    Mod+WheelScrollDown { focus-workspace-down; }
    Mod+WheelScrollUp   { focus-workspace-up; }
    Mod+Shift+WheelScrollDown { move-window-to-workspace-down; }
    Mod+Shift+WheelScrollUp   { move-window-to-workspace-up; }
}
